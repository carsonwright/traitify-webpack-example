var Underscore;

Underscore = require('underscore');

module.exports = {
  unique: Underscore.uniq,
  shuffle: function(o) {
    var i, j, x;
    j = void 0;
    x = void 0;
    i = o.length;
    while (i) {
      j = Math.floor(Math.random() * i);
      x = o[--i];
      o[i] = o[j];
      o[j] = x;
    }
    return o;
  }
};
