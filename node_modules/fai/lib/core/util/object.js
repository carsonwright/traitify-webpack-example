var Underscore;

Underscore = require('underscore');

module.exports = {
  extend: Underscore.extend,
  isEqual: Underscore.isEqual,
  isDict: function(o) {
    return ﬁ.util.isObject(o) && !ﬁ.util.isArray(o) && !ﬁ.util.isFunction(o) && !ﬁ.util.isString(o) && !ﬁ.util.isNumber(o);
  },
  isEmpty: function(o) {
    var k, v;
    if (!this.isDict(o)) {
      return false;
    }
    for (k in o) {
      v = o[k];
      if (Object.prototype.hasOwnProperty.call(o, k)) {
        return false;
      }
    }
    return true;
  },
  clone: function(target) {
    var e, error;
    try {
      target = JSON.parse(JSON.stringify(target));
    } catch (error) {
      e = error;
      throw new ﬁ.error("Cannot clone given element. " + e.message);
    }
    return target;
  }
};
